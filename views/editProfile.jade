extends layout
 
block content
  h1 #{title}
  div.container
    div.row
      div.col-sm-6.col-md-4.col-md-offset-4
        h1.text-center.login-title Sign in to our Passport app
          div.account-wall
            form(class='form-signup', action='/signup', method='POST')
              input(type='text', name='email' class='form-control', placeholder='#{user.email}',required, autofocus)
              input(type='password', name='new_password' class='form-control', placeholder='New Password', required)
              input(type='password', name='new_password_confirm' class='form-control', placeholder='new Password confirm', required)
              input(type='password', name='old_password' class='form-control', placeholder='old Password confirm', required)
              button(class='btn btn-lg btn-primary btn-block', type='submit') Edit Account
            span.clearfix
          #message
            if message
              h1.text-center.error-message #{message}
